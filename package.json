{
  "name": "info-class-svg",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "node scripts/generate-all.js",
    "validate": "node scripts/validate-svg.js",
    "build": "node scripts/generate-all.js && node scripts/validate-svg.js",
    "test": "node scripts/validate-svg.js",
    "deploy": "node scripts/generate-all.js && node scripts/validate-svg.js && git add -A && git commit -m 'update icons' && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@tabler/icons": "^3.37.1",
    "lucide-static": "^0.575.0"
  }
}
